export const hourGlass = (level) => {
  // Implement a function the return a hour glass
  // as a string

  // The level parameter defines how many levels the
  // tree should contain

  // Example a hour glass with 3 levels
  //* * *
  // * *
  //  *  
  // * *
  //* * *

  // If no level is provided the function should throw an error

};
